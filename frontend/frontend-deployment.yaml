apiVersion: apps/v1
kind: Deployment
metadata:
  name: groovelab-frontend
  namespace: groovelab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: groovelab-frontend
  template:
    metadata:
      labels:
        app: groovelab-frontend
    spec:
      containers:
        - name: frontend
          image: arunhotra/groovelab-frontend:b26a8362f5078893f992af4b22a1f82613c037b6
          imagePullPolicy: Always
          # Frontend was built with VITE_API_BASE=/api in CI
          ports:
            - containerPort: 5173
          resources: {}
---
apiVersion: v1
kind: Service
metadata:
  name: groovelab-frontend
  namespace: groovelab
spec:
  type: ClusterIP
  selector:
    app: groovelab-frontend
  ports:
    - port: 80
      targetPort: 80
